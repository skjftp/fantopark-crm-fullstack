# Firebase Configuration (Choose one method - see FIREBASE_CREDENTIALS_SETUP.md)
# Method 1: Path to service account JSON file (recommended for local dev)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account-key.json
# Method 2: Base64 encoded service account JSON (good for CI/CD)
# GOOGLE_CREDENTIALS_BASE64=your-base64-encoded-json-here
# Project ID (required)
GOOGLE_CLOUD_PROJECT=your-project-id

# JWT Configuration
JWT_SECRET=your-secret-key-change-this
JWT_EXPIRE=7d  # Token expiry time (examples: 7d, 30d, 24h, 2h)

# Server Configuration
PORT=8080

# Meta/Facebook Configuration (optional)
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret
META_ACCESS_TOKEN=your-meta-access-token
META_WEBHOOK_VERIFY_TOKEN=your-webhook-verify-token

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Environment
NODE_ENV=development